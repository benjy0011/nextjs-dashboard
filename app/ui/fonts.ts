import { Inter, Lusitana, Pixelify_Sans } from 'next/font/google'

export const inter = Inter({ subsets: ['latin'] });

export const pixelifySans = Pixelify_Sans({ subsets: ['latin'] });

export const lusitana = Lusitana({
  subsets: ['latin'],
  weight: ['400', '700']
});
